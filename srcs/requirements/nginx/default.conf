server 
{
    listen 443;
    ssl_protocols TLSv1.2 TLSv1.3;
    client_max_body_size 100000;
    server_name ${DOMAIN_NAME};
    
    location / {
        limit_except GET {
            deny all;
        }
    }
}